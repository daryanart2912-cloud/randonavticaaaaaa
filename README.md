# Квантовая Рандонавтика (Vibe Randonautica)

Telegram Mini App: React + Vite + Tailwind + Leaflet.

```bash
npm install
npm run dev
```

Сборка: `npm run build`. Для Mini App укажите HTTPS-URL `dist` в BotFather (`/newapp`).
